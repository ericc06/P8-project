{% extends 'base.html.twig' %}

{% block header_title %}
    <h1>{{ 'welcome_message'|trans }}</h1>
{% endblock %}

{% block body %}

    <div class="row">
        <a href="{{ path('task_create') }}" class="btn btn-success">{{ 'create_a_new_task'|trans }}</a>
        <a href="{{ path('task_list') }}" class="btn btn-info">{{ 'view_tasks_to_be_done'|trans }}</a>
        <a href="" class="btn btn-secondary">{{ 'view_completed_tasks'|trans }}</a>
    </div>
{% endblock %}
