{% extends 'base.html.twig' %}

{% block header_title %}<h1>{{ 'create_a_user'|trans }}</h1>{% endblock %}
{% block header_img %}{% endblock %}

{% block body %}
    <div class="row">
        {{ form_start(form, {'action' : path('user_create')}) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-success pull-right">{{ 'add'|trans }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
